/**
 * Returns the current color scheme for the web version.
 * @returns {string} The current color scheme, either 'light' or 'dark'.
 */
// This function returns the color scheme for the web version
// Currently, it always returns 'light' as the default color scheme
export function useColorScheme() {
  return 'light';
}
