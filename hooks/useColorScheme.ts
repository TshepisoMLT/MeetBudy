/**
 * Exports the `useColorScheme` hook from the `react-native` library.
 * This hook can be used to determine the user's current color scheme preference (e.g. light or dark mode).
 */
// Export the useColorScheme hook from react-native
export { useColorScheme } from 'react-native';
